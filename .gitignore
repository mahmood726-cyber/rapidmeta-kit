# Built dashboards are regenerable (and large). Ignore the output folder,
# but the template/ engine + assets MUST stay in git (they are the kit).
output/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node (build-time minifier deps; never part of the shipped offline kit)
node_modules/

# Dev/scratch helpers
_*.py
sentinel-findings.md
sentinel-findings.jsonl

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
